# คู่มือการแปลงไฟล์ Markdown (.md) เป็น HTML (.html)

เอกสารนี้อธิบายวิธีการแปลงไฟล์ Markdown เช่น `DEPLOYMENT.md` ให้กลายเป็นไฟล์ HTML ที่สามารถเปิดดูบนเบราว์เซอร์ได้ ซึ่งมีหลายวิธีให้เลือกใช้ตามความสะดวก

---

### วิธีที่ 1: ใช้เครื่องมือแปลงออนไลน์ (ง่ายที่สุด)

วิธีนี้เหมาะสำหรับผู้ที่ต้องการความรวดเร็วและไม่ต้องการติดตั้งโปรแกรมใดๆ เพิ่มเติม

1.  **เปิดเว็บไซต์:** เข้าไปที่เว็บไซต์สำหรับแปลงไฟล์ เช่น [Markdown to HTML Converter](https://www.markdowntohtml.com/)
2.  **คัดลอกเนื้อหา:** เปิดไฟล์ `DEPLOYMENT.md` ในเครื่องของคุณ แล้วคัดลอก (Copy) เนื้อหาทั้งหมด
3.  **วางเนื้อหา:** นำเนื้อหาที่คัดลอกไปวาง (Paste) ในช่อง Markdown (ฝั่งซ้าย) ของเว็บไซต์
4.  **คัดลอก HTML:** เว็บไซต์จะสร้างโค้ด HTML ให้โดยอัตโนมัติในฝั่งขวา ให้คุณคัดลอกโค้ด HTML ทั้งหมด
5.  **บันทึกเป็นไฟล์:**
    *   เปิดโปรแกรม Text Editor (เช่น Notepad, VS Code, Sublime Text)
    *   สร้างไฟล์ใหม่แล้ววางโค้ด HTML ที่คัดลอกมา
    *   บันทึกไฟล์โดยตั้งชื่อเป็น `DEPLOYMENT.html` (สำคัญที่ต้องลงท้ายด้วย `.html`)

---

### วิธีที่ 2: ใช้เครื่องมือผ่าน Command Line (สำหรับนักพัฒนา)

เนื่องจากโปรเจคนี้ใช้ Node.js อยู่แล้ว เราสามารถติดตั้งเครื่องมือที่ชื่อว่า `marked` เพื่อทำการแปลงไฟล์ได้อย่างรวดเร็วผ่าน command line

1.  **ติดตั้ง `marked`:**
    *   เปิด Terminal หรือ Command Prompt ขึ้นมา
    *   รันคำสั่งต่อไปนี้เพื่อติดตั้ง `marked` แบบ global (ติดตั้งครั้งเดียวใช้ได้ตลอด):
        ```bash
        npm install -g marked
        ```

2.  **ทำการแปลงไฟล์:**
    *   ใช้ `cd` เพื่อเข้ามายัง Directory ของโปรเจค (`D:\005_ComOA`)
    *   รันคำสั่งต่อไปนี้:
        ```bash
        marked -i DEPLOYMENT.md -o DEPLOYMENT.html
        ```
    *   **คำอธิบาย:**
        *   `marked`: เป็นการเรียกใช้โปรแกรม
        *   `-i DEPLOYMENT.md`: ระบุไฟล์ต้นทาง (input)
        *   `-o DEPLOYMENT.html`: ระบุไฟล์ผลลัพธ์ (output)

    หลังจากรันคำสั่งนี้ คุณจะได้ไฟล์ใหม่ชื่อ `DEPLOYMENT.html` อยู่ในโฟลเดอร์โปรเจคของคุณทันที

---

### วิธีที่ 3: ใช้ส่วนขยาย (Extension) ใน VS Code

หากคุณใช้โปรแกรม Visual Studio Code (VS Code) อยู่แล้ว วิธีนี้จะสะดวกมาก

1.  **ติดตั้งส่วนขยาย:**
    *   เปิด VS Code
    *   ไปที่หน้าต่าง Extensions (ไอคอนรูปสี่เหลี่ยมซ้อนกัน)
    *   ค้นหาคำว่า `Markdown All in One` แล้วกด Install

2.  **แปลงไฟล์:**
    *   เปิดไฟล์ `DEPLOYMENT.md` ใน VS Code
    *   คลิกขวาที่พื้นที่ว่างในหน้า editor
    *   เลือกเมนู `Markdown All in One: Print current document to HTML`
    *   VS Code จะสร้างและเปิดไฟล์ HTML ให้คุณโดยอัตโนมัติ คุณสามารถกด `Ctrl+S` เพื่อบันทึกไฟล์นี้เป็น `DEPLOYMENT.html` ได้เลย
